server.port: 8082

spring:
  application.name: cowork-gateway
  datasource:
    url: jdbc:h2:mem:testdb;
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml
    enabled: true
    drop-first: true
  h2:
    console:
      enabled: true

logging:
  level:
    org.springframework.web: INFO
    org.springframework.data.elasticsearch.client.WIRE: trace

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always

SECRET_KEY: 4caa0955b0d45c4ec46119550919ca270df25c2b77c6c2fe1a64b4f1fc20db46